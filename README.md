# Mikoshi

> *"Wake up Samurai, we've got a city to burn."*

A **dendritic-pattern library flake** — a batteries-included NixOS configuration shipped not as a set of hosts, but as a tree of small, self-contained, independently-toggleable **aspects** that any consuming flake can import and switch on. Point your own flake at mikoshi, enable the aspects you want under the `mikoshi.*` option namespace, and you get a fully configured desktop (window manager, launcher, bar, terminal, Neovim, theming, audio, fonts, ...) without copying any of it into your own repo.

Built on [flake-parts](https://github.com/hercules-ci/flake-parts) and [import-tree](https://github.com/denful/import-tree): every file under `modules/` is auto-discovered, and each file contributes named modules to `flake.modules.nixos.*` and `flake.modules.homeManager.*`. There is no central imports list to maintain — dropping a new `.nix` file into `modules/` is all it takes.

## The dendritic pattern

Each file in `modules/` is one **aspect**: a single concern (a window manager, a launcher, garbage collection, gaming support) that carries *both* its NixOS-level and home-manager-level configuration in one place, wired together like so:

```nix
{config, ...}: let
  hmFor = config.flake.lib.hmFor;
  hmClass = config.flake.modules.homeManager;
in {
  flake.modules.nixos.thing = {config, lib, pkgs, ...}: let
    cfg = config.mikoshi.thing;
  in {
    options.mikoshi.thing.enable = lib.mkEnableOption "thing";
    config = lib.mkIf cfg.enable {
      home-manager.users = hmFor config.mikoshi.meta.users hmClass.thing;
      # ... system-level config ...
    };
  };

  flake.modules.homeManager.thing = {...}: {
    # ... per-user config ...
  };
}
```

Key properties:

- **Everything is gated.** Importing an aspect does nothing until you set its `mikoshi.<name>.enable` (a few quality-of-life aspects like tmux, nvf, networking, and GC default to `true`, all overridable).
- **Aspects compose sideways.** Enabling `mikoshi.wm.sway` pulls in `mikoshi.walker` via `mkDefault`; enabling `mikoshi.wm.hyprland` pulls in `mikoshi.graphical` and `mikoshi.waybar`. You can still override any of those defaults.
- **home-manager is wired automatically.** You declare *who* your users are once (`mikoshi.meta.users`), and every enabled aspect applies its home-manager half to those users via the `hmFor` helper (`modules/lib.nix`).
- **Multiple files can extend the same aspect.** `audio.nix`, `fonts.nix`, and `ghostty.nix` all contribute to the `graphical` aspect; flake-parts merges them.

## Using it

```nix
{
  inputs.mikoshi.url = "github:szewczyk-bartosz/mikoshi";

  outputs = {nixpkgs, mikoshi, ...}: {
    nixosConfigurations.myhost = nixpkgs.lib.nixosSystem {
      system = "x86_64-linux";
      modules = [
        mikoshi.modules.nixos.default   # imports every aspect (all still opt-in)
        ./hardware-configuration.nix
        {
          mikoshi.meta.users = ["samurai"];
          mikoshi.wm.hyprland.enable = true;
          mikoshi.gaming.enable = true;

          users.users.samurai.isNormalUser = true;
          system.stateVersion = "26.05";
        }
      ];
    };
  };
}
```

`mikoshi.modules.nixos.default` aggregates every aspect (`modules/default.nix`); you can instead import individual aspects (`mikoshi.modules.nixos.sway`, `.gaming`, ...) if you want a smaller closure of options.

> **Note:** this flake's own `nixosConfigurations` output is *not* a set of real hosts — it's the per-aspect test systems generated by `modules/checks.nix` (see [Checks](#checks--development)).

## Inputs

| Input | Purpose |
|---|---|
| `nixpkgs` | `nixos-26.05` |
| `flake-parts` + `import-tree` | The dendritic skeleton: module auto-discovery and the `flake.modules.*` output |
| `home-manager` | Per-user config, wired as a NixOS module (`useGlobalPkgs`, `useUserPackages`) |
| `nvf` + `neovim-nightly-overlay` | Declarative Neovim (nightly) — see [Neovim](#neovim-nvf) |
| `stylix` | System-wide base16 theming (**currently not working** — under evaluation, hard-disabled in `modules/stylix.nix`) |
| `bmd` | [engramware](https://github.com/szewczyk-bartosz/engramware) package |

## Aspects

### Always-on-by-default (overridable)

| Option | File | What it does |
|---|---|---|
| `mikoshi.meta.users` | `options.nix` | The users every aspect's home-manager config applies to (required — evaluation fails if unset) |
| `mikoshi.meta.keyboardLayouts` | `options.nix` | XKB layouts, used by console/X/GNOME (default `["gb"]`) |
| `mikoshi.network.enable` | `network.nix` | NetworkManager |
| `mikoshi.nixGarbageCollection.enable` | `nix-garbage-collection.nix` | Weekly `nix gc`, 30-day retention, min/max-free thresholds |
| `mikoshi.nvf.enable` | `nvf.nix` | The full Neovim setup below |
| `mikoshi.tmux.enable` | `tmux.nix` | tmux: `C-Space` prefix, vi keys, mouse, `h/j/k/l` pane navigation, `a` for last window |
| *(base)* | `base.nix` | home-manager wiring, flakes enabled, locale/timezone/console defaults (`mkDefault`, override freely) |

### Desktops (pick one)

| Option | File | What it does |
|---|---|---|
| `mikoshi.wm.hyprland.enable` | `hyprland.nix` | Hyprland via UWSM + greetd/tuigreet, wofi launcher, swayosd, custom alt-tab workspace-toggle daemon, hyprpaper, polkit agent, screenshots via grimblast. Options for monitors, wallpaper, keyboard, `mainMod` (default `ALT`), launcher keybind (bare `Super` tap), terminal, file manager |
| `mikoshi.wm.sway.enable` | `sway.nix` | Sway with hyprland-parity keybinds (Alt as `$mod`, bare `Super_L` tap opens walker), foot, kanshi, swayidle, swayosd, grimblast, polkit agent. Pulls in `mikoshi.walker` by default |
| `mikoshi.wm.gnome.enable` | `gnome.nix` | GNOME (core apps trimmed, Pop Shell tiling), Adwaita GTK/Qt/cursor theming, dark mode, Alt-based workspace keybinds via dconf |
| `mikoshi.wm.plasma.enable` | `plasma.nix` | KDE Plasma 6 + SDDM, Krohnkite tiling, Konsole excluded (ghostty ftw) |

### Desktop surfaces & utilities

| Option | File | What it does |
|---|---|---|
| `mikoshi.graphical.enable` | `graphical.nix` (+ `audio.nix`, `fonts.nix`, `ghostty.nix`) | Shared desktop base: PipeWire, Noto/JetBrainsMono Nerd Font fontconfig, ghostty terminal (Catppuccin Mocha, custom cursor shaders). Auto-enabled by hyprland/gnome/plasma |
| `mikoshi.waybar.enable` | `waybar.nix` | Waybar as a user service: workspaces, clock, tray, pulseaudio, power button, optional battery (`mikoshi.waybar.battery.enable`) |
| `mikoshi.walker.enable` | `walker.nix` | [Walker](https://github.com/abenz1267/walker) launcher backed by an [elephant](https://github.com/abenz1267/elephant) user service. Providers: apps (default), calculator (`=`), files (`/`), clipboard (`:`) |
| `mikoshi.stylix.enable` | `stylix.nix` | base16 theming via stylix; picks from `themes/*.yaml` first (asakara, mocha-dim, dark-violet, purple-dream), falls back to `base16-schemes`. **Currently not working** — stylix is being evaluated and is hard-disabled internally, so this option has no effect for now |

### Extras

| Option | File | What it does |
|---|---|---|
| `mikoshi.gaming.enable` | `gaming.nix` | Steam, gamemode, 32-bit graphics, scx (`scx_bpfland`) scheduler, protonup-qt, Heroic |
| `mikoshi.obs-amd.enable` | `obs.nix` | OBS Studio with AMD VAAPI hardware encoding (`radeonsi` + obs-vaapi) |
| `mikoshi.bmd.enable` | `bmd.nix` | The `bmd` package from engramware |

## Neovim (nvf)

`mikoshi.nvf.enable` (on by default) builds a full Neovim setup with **nightly Neovim**, `vim` aliased to `nvim`, and `$EDITOR` set. Leader is **Space**. which-key is enabled, so most of these discover themselves as you type.

Plugins: telescope, flash, aerial, neo-tree (floating), noice, colorizer, mini.ai/statusline, autopairs, indent-blankline, dashboard, comment.nvim, gitsigns, git-conflict, toggleterm (+ floating lazygit), bufferline, blink-cmp (+ friendly-snippets), full LSP + treesitter + formatters for **Nix, Python, Rust, Java, JS/TS, CSS, HTML** (Java/C treesitter too). Catppuccin Mocha theme, system clipboard via wl-copy.

### General

| Key | Mode | Action |
|---|---|---|
| `jk` | insert | Escape to normal mode |
| `s` | normal | flash.nvim jump (labelled motion) |
| `<leader>e` | normal | Toggle neo-tree file explorer (floating) |
| `<leader>lo` | normal | Toggle aerial symbol outline |
| `<F12>` | normal | Toggle floating terminal (toggleterm; lazygit integration runs floating) |
| `<Esc>` | terminal | Exit terminal-insert mode |

### Windows & splits

| Key | Action |
|---|---|
| `<C-h>` / `<C-j>` / `<C-k>` / `<C-l>` | Move to left / bottom / top / right split |
| `<leader>sh` | Split horizontally |
| `<leader>sv` | Split vertically |

### Buffers (bufferline)

| Key | Action |
|---|---|
| `<Tab>` / `<S-Tab>` | Cycle next / previous buffer |
| `<leader>bq` | Close current buffer |
| `<leader>bc` | Pick buffer |
| `<leader>bsd` / `<leader>bse` / `<leader>bsi` | Sort by directory / extension / id |
| `<leader>bmn` / `<leader>bmp` | Move buffer next / previous |

### LSP

| Key | Action |
|---|---|
| `<leader>ld` | Go to definition |
| `<leader>lr` | List references |
| `<leader>li` | List implementations |
| `<leader>lt` | Go to type definition |
| `<leader>ln` | Rename symbol |
| `<leader>la` | Code action |
| `<leader>ls` | Document symbols |
| `<leader>lh` | Hover documentation |
| `<leader>lf` | Format buffer (format-on-save is off) |

### Diagnostics

| Key | Action |
|---|---|
| `<leader>dd` | Open diagnostic float |
| `<leader>dn` / `<leader>dp` | Next / previous diagnostic |

### Git (gitsigns)

| Key | Action |
|---|---|
| `]c` / `[c` | Next / previous hunk |
| `<leader>gs` / `<leader>gu` | Stage / undo-stage hunk |
| `<leader>gp` | Preview hunk |
| `<leader>gS` / `<leader>gR` | Stage / reset whole buffer |
| `<leader>gb` | Blame line |
| `<leader>gtb` / `<leader>gtd` | Toggle blame / toggle deleted |
| `<leader>gdd` / `<leader>gdp` | Diff this / diff project |

### Git conflicts (git-conflict)

| Key | Action |
|---|---|
| `<leader>gco` / `<leader>gct` | Take ours / theirs |
| `<leader>gcb` / `<leader>gcn` | Take both / none |
| `]x` / `[x` | Next / previous conflict |

### Completion (blink-cmp)

| Key | Action |
|---|---|
| `<Tab>` / `<S-Tab>` | Next / previous suggestion |
| `<CR>` | Confirm |
| `<Esc>` | Close menu |

### Dashboard

On the start screen: `f` find file, `r` recent files, `g` live grep (all Telescope).

## Desktop keybinds (Hyprland / Sway)

Both desktops share the same scheme: **Alt** (`mainMod`/`$mod`) drives window management, and a **bare Super tap** opens the launcher (wofi on Hyprland, walker on Sway).

| Key | Action |
|---|---|
| `Super` (tap) | Launcher |
| `Alt+Return` | Terminal (ghostty on Hyprland, foot on Sway) |
| `Alt+Shift+Q` | Kill window |
| `Alt+V` | Toggle floating |
| `Alt+H/J/K/L` | Focus left/down/up/right |
| `Alt+Shift+H/J/K/L` | Move window |
| `Alt+1..9,0` | Workspace 1–10 |
| `Alt+Shift+1..9,0` | Move window to workspace 1–10 (silent on Hyprland; Sway has no silent variant and follows focus — [swaywm/sway#1518](https://github.com/swaywm/sway/issues/1518)) |
| `Alt+Tab` | Toggle to previous workspace (Hyprland, via the bundled alt-tab daemon) |
| `Alt+Shift+S` | Area screenshot → clipboard + `~/Pictures/Screenshots/` (grimblast) |
| `Super`+drag | Move (left) / resize (right) floating windows |
| Volume / brightness keys | swayosd on-screen display |
| Media keys | playerctl |

## Checks & development

`modules/checks.nix` generates, for **every** NixOS aspect, a minimal container-flavoured `nixosSystem` with that aspect force-enabled and a test user, and exposes each as a flake check — so `nix flake check` proves every aspect at least evaluates and builds its toplevel derivation in isolation.

A pre-commit hook runs exactly that:

```sh
git config core.hooksPath .githooks   # once, then every commit runs `nix flake check`
```

Repo layout:

```
flake.nix          # inputs + one-liner: flake-parts.mkFlake over import-tree ./modules
modules/           # every file auto-imported; one aspect per concern
themes/            # custom base16 schemes for stylix
todo.md            # honest backlog
```

## License

[GPL-3.0](LICENSE)
